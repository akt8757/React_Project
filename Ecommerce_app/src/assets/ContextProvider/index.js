import { createContext } from "react";

// context for main productlist array
export const renderContext = createContext();

// Filtering Context
export const filterContext = createContext();

// mutable Context
export const mutableContext = createContext();
